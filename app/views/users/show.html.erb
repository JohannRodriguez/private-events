<%= invalid_event %>
<h1>Your Events</h1>
<h2><%= @user.username %><h2>
<% @user.events.each do |e| %>
  <h4>
    <%= link_to full_event_name(e), events_show_path(:id => e.id) %>
  </h4>
<% end %>
<%= link_to  "New Event", events_new_path %>
